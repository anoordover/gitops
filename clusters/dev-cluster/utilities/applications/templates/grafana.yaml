

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: monitoring
    server: {{ .Values.spec.destination.server }}
  project: default
  sources:
    - repoURL: 'https://grafana.github.io/helm-charts'
      targetRevision:  {{ .Values.spec.grafana.helmchartVersion }}
      chart: grafana
      helm:
        valueFiles:
        - {{ printf "$values/clusters/%s/utilities/values/grafana-values.yaml" .Values.spec.cluster | quote }}
    - repoURL: 'https://nc-nl-internships@dev.azure.com/nc-nl-internships/NCNL-ArchitectureBoard/_git/DependencyTrack-GitOps'
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true

