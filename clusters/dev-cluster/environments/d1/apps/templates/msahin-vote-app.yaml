apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: azure-vote-front
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    namespace: msahin025
    server: {{ .Values.spec.destination.server }}
  project: default
  sources:
    - repoURL: 'registry-1.docker.io/msahin025'
      targetRevision:  {{ .Values.spec.msahin025.helmchartVersion }}
      helm:
        valueFiles:
        - {{ printf "$values/clusters/%s/environments/d1/values/msahin-values.yaml" .Values.spec.cluster | quote }}
    - repoURL: 'https://github.com/daseth/gitops.git'
      targetRevision: main
      ref: values
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
